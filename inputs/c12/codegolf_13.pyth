#!/bin/bash

read -d '' ARG <<"EOF"
'> <vv    <
 v ^ >v v 
  >v^^>vv^
    ^>^ v 
>  v<v  >>
  >v v<^  '
2
3
EOF


read -d '' SCRIPT <<"EOF"
CODEGOLF_METHOD_PLACEHOLDER

EOF


echo "$ARG" | python3 pyth.py -s -c "$SCRIPT"

